import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.maninchair',
  appName: 'maninchair',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  },
  
};

export default config;
